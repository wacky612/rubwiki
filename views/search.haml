-# coding: utf-8
%h1.title #{keyword} の検索結果
%ul
  - result.each do |path|
    - if File.extname(path) == RubWiki::App::DefaultExt
      %li<
        %a{ href: "#{baseurl}/#{path[0...-3]}" }< #{path[0...-3]}
